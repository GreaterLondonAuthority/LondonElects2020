langcode: en
status: true
dependencies:
  config:
    - user.role.edit_media
  module:
    - user
id: user_add_role_action.edit_media
label: 'Add the Create / edit media role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: edit_media
