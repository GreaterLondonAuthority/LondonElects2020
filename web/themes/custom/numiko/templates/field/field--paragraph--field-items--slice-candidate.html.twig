{% set column_count = (columns is defined and columns) ? columns : 3 %}
<div{{ attributes.addClass('l-grid l-grid--' ~ column_count ~ '-col l-grid--w-v-gutter l-grid--w-h-gutter') }}>
  {% for item in items %}
    <div class="l-grid__item">
      {{ item.content }}
    </div>
  {% endfor %}
</div>
